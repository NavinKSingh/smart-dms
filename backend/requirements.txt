# Flask - the web framework
Flask==3.0.3

# Flask-SQLAlchemy - ORM to interact with MySQL using Python classes
Flask-SQLAlchemy==3.1.1

# Flask-Migrate - handles database migrations
Flask-Migrate==4.0.7

# Flask-JWT-Extended - handles login tokens (JWT authentication)
Flask-JWT-Extended==4.6.0

# Flask-CORS - allows frontend (React) to talk to backend (Flask)
Flask-Cors==4.0.1

# mysql-connector-python - MySQL database driver
mysql-connector-python==8.4.0

# PyMySQL - alternative MySQL driver (used by SQLAlchemy)
PyMySQL==1.1.1

# Werkzeug - password hashing and file utilities (comes with Flask)
Werkzeug==3.0.3

# python-dotenv - loads .env files for secrets
python-dotenv==1.0.1

# Pillow - image processing (for image preview support)
Pillow==10.4.0